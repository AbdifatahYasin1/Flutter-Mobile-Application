import 'dart:math';

//  2. **Math Functions:**
//    - `import math` allows you to use functions like `min(a, b)`, `max(a, b)`, `pow(base, exponent)`, and more.

void main() {
  print(min(2, 9));
  print(max(2, 9)); 
  print(pow(4, 4)); // 4^4
}
